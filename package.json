{
  "name": "findbest",
  "version": "0.0.1",
  "description": "Sort and export Instagram and TikTok feeds. Manifest V3 Chrome extension.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.1.42",
    "typescript": "^6.0.0",
    "vite": "^8.0.0"
  }
}
